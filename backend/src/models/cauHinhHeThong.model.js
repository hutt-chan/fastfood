const db = require('../config/database');

// TODO: viết các hàm query cho cauHinhHeThong
// Ví dụ:
// const findById = (id) => db.execute('SELECT * FROM ... WHERE id = ?', [id]);

module.exports = {};
